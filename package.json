{
  "name": "babel-typescript",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "run-s clean build:*",
    "build:ts": "babel --extensions \".ts\" src --out-dir dist",
    "build:js": "babel src --out-dir dist"
  },
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.4",
    "@babel/preset-env": "^7.4.4",
    "@babel/preset-typescript": "^7.3.3",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.6.3"
  }
}
